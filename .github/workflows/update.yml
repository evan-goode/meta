name: "update"
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: cachix/install-nix-action@v20
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - uses: cachix/cachix-action@v12
      with:
        name: unmojang
        authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
    - run: |
        ssh-add <(echo "$UPSTREAM_KEY")
        ssh-add <(echo "$LAUNCHER_KEY")
    - run: |
        ./clone.sh
        nix run
